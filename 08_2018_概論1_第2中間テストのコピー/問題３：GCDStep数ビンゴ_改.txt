#### __問題文__
***
3×3のマスgrid[3][3]に自然数が配置されている．
また，標準入力から自然数 b が与えられる．
grid[3][3]の各マスの自然数と bの最大公約数(GCD)を
ユークリッドの互除法を用いて求めるのに必要な
ステップ数（剰余演算回数）をsteps[3][3]に代入する
プログラムの再帰呼び出し関数gcd_stepsを作成しなさい．．

#### __入力される値__
マスに配置された 3×3 の自然数（9個の整数）
対象となる自然数 b（1個）


#### __期待される出力値__
各マスに対するユークリッド互除法のステップ数（int型の3×3配列）

***
####__初期ソースコード__
（コピー＆ペーストしてもよい，cout文は変更しないこと）
```
#include <iostream>
#include <climits>

using namespace std;

// ユークリッドの互除法のステップ数を導出
int gcd_steps(int a, int b, int steps = 0) {
    /*ここに回答を記入*/
}

int main() {
    int grid[3][3];      // 3x3のマス
    int steps[3][3];     // 各マスのステップ数
    int b;

    // マスの入力
    cout << "ビンゴのマスを入力（3x3）:" << endl;
    for (int i = 0; i < 3; i++)
        for (int j = 0; j < 3; j++)
            cin >> grid[i][j];

    // bの入力
    cout << "bを入力: "<< endl;
    cin >> b;

    // 各マスに対するステップ数を計算
    cout << "ステップ数配列:" << endl;
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++) {
            //a < bの場合は，剰余数を逆にする
            if (grid[i][j] > b) {
                steps[i][j] = gcd_steps(grid[i][j], b);
            }
            else {
                steps[i][j] = gcd_steps(b,grid[i][j]);
            }
            cout << steps[i][j] << " ";
        }
        cout << endl;
    }
    return 0;
}
```